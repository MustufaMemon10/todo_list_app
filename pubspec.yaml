name: todo_list_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get:
  cupertino_icons:
  hive:
  hive_flutter:
  path_provider:
  flutter_local_notifications: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner:
  hive_generator:

flutter:

  uses-material-design: true
